# ── Core Framework ─────────────────────────────────────────────────────────
Flask>=3.0.0
python-dotenv>=1.0.1
python-docx>=1.1.2
PyMuPDF>=1.24.0               # Renders Aadhaar PDF pages for multimodal OCR

# ── Database ────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9        # PostgreSQL driver with connection pool support

# ── AI Providers ────────────────────────────────────────────────────────────
google-generativeai>=0.8.0    # Gemini (dev + prod option)
google-genai>=1.0.0           # Current Gemini SDK for multimodal Aadhaar OCR
boto3>=1.35.0                 # AWS Bedrock (prod option)

# ── Production WSGI Server ──────────────────────────────────────────────────
gunicorn>=21.2.0              # Multi-worker WSGI server (production)
gevent>=23.9.1                # Async gevent worker for concurrent request handling

# ── Scalability & Security ──────────────────────────────────────────────────
Flask-Limiter>=3.5.0          # Rate limiting (DoS protection on preview endpoint)
cachetools>=5.3.0             # In-memory TTL cache for preview responses
Flask-Cors>=4.0.0             # CORS headers for mobile app API access

# ── Optional: upgrade to Redis-backed cache for multi-node production ───────
# redis>=5.0.0
# limits[redis]>=3.5.0
